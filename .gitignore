# Local wrangler state
.wrangler/
.dev.vars
.dev.vars.*

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp
*~

# Secrets — never commit these
.env
.env.*
secrets.toml

# Raw API response artifacts (regenerate from source commands in README)
response.bin
raw-response.bin

# Node
node_modules/
# package-lock.json is intentionally committed for reproducible builds (when present).
