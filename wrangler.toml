name = "skill-portal"
main = "skill-portal-worker.js"
compatibility_date = "2026-06-18"

# usage_model is configured in the Cloudflare dashboard
# (Workers & Pages → skill-portal → Settings → Usage Model).
